# Optional semantic enhancement for the semantic-video-edit example.
# The workflow still runs with deterministic motion/audio scoring when unset.
MINIMAX_API_KEY=your-minimax-subscription-key

# Other example Agents may use an OpenAI-compatible endpoint.
OPENAI_API_KEY=your-openai-compatible-key
